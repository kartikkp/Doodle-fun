# Publish the verified app through existing GitHub Pages

## Objective
Ensure the merged public site serves the same self-contained package that passed activity/offline QA.

## Context
PR #2 merged. Live Pages main/root serves24 activities successfully online (288 launch checks), but root sw.js is absent because the built dist output is ignored. Existing Pages configuration remains main/root.

## Tasks
1. Retain editable source as app-shell.html; generate and commit root index.html/sw.js identical to dist. Add CI gate rejecting stale generated artifacts.
2. Serve only the root pair under /Doodle-fun/, verify worker scope, stop server, reload and open every card in both engines. Prepare follow-up PR; recheck live after merge.

## Success
Public deployment receives the tested standalone files without a Pages configuration change. Runtime fingerprint remains 387b68987e207d18; new regression passes. Production verification awaits the follow-up merge.

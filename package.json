{
  "name": "doodle-fun",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A touch-first creative learning playground for ages 2–10.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test",
    "check": "node --check app.js && node --check core.js && node --check draw.js && node --check learning.js"
  },
  "devDependencies": { "@playwright/test": "^1.55.0" }
}
